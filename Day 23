#include <stdio.h>

int main(void) {
	int t,w,x,y,z;
	scanf("%d",&t);
	while(t--){
	    scanf("%d %d %d %d",&w,&x,&y,&z);
	    if(w+(y*z)==x)
	    printf("filled\n");
	    else if(w+(y*z)>x)
	    printf("overflow\n");
	    else if(w+(y*z)<x)
	    printf("unfilled\n");
	}
	return 0;
}

