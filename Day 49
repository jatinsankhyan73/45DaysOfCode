#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--){
	    int n,count=0,A;
	    scanf("%d",&n);
	    for(int i=0;i<n;i++){
	        scanf("%d",&A);
	        if(A>=10 && A<=60){
	            count++;
	        }
	    }
	        printf("%d\n",count);
	    
	}
	return 0;
}
