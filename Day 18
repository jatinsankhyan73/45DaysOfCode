#include <stdio.h>

int main(void) {
	int a,b,t;
	scanf("%d",&t);
	while(t--){
	    scanf("%d %d",&a,&b);
	    if(a<=b)
	    printf("%d\n",7-b);
	    else if (a>b)
	    printf("%d\n",7-a);
	}
	return 0;
}

