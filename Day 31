#include <stdio.h>

int main(void) {
	int t,n,x,y;
	scanf("%d",&t);
	while(t--){
	    scanf("%d %d %d",&n,&x,&y);
	    if(y%x==00)
	        printf("YES\n");
	    else 
	        printf("NO\n");
	}
	return 0;
}
