#include <stdio.h>

int main(void) {
	int T,N,X,Y;
	scanf("%d",&T);
	while(T--){
	    scanf("%d %d",&N,&X);
	    Y=N-X;
	    if(X==0 || N==X)
	        printf("0\n");
	    else if(X<=Y)
	        printf("%d\n",X);
	    else if (X>Y)
	        printf("%d\n",Y);
	}
	return 0;
}

