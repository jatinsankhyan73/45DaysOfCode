#include <stdio.h>

int main(void) {
	int t,x,y,a;
	scanf("%d",&t);
	while(t--){
	    scanf("%d %d",&x,&y);
	    a=y-x;
	        if (a%8==0)
	            printf("%d\n",a/8);
	        else 
	            printf("%d\n",a/8+1);
	    
	}
	return 0;
}

