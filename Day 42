#include <stdio.h>

int main(void) {
	int t,p,q;
	scanf("%d",&t);
	while(t--){
	    scanf("%d %d",&p,&q);
	    if ((p+q)%4==2 || (p+q)%4==3 || p+q==2 ||p+q==3) {
	        printf("Bob\n");
	    }
	    else if((p+q)%4<=1 || p+q<2 ){
	        printf("Alice\n");
	    }
	   
	}
	return 0;
}
