#include <stdio.h>

int main(void) {
	int t, n, m, e, c;
	scanf("%d", &t) ;
	while(t--){
	    scanf("%d %d ", &n, &m) ;
	    c=n/m;
	    e=c%2;
	    if (n%m==0 && e== 0)
	        printf("Yes\n");
	    else 
	        printf("No\n");
	}
	
	return 0;
}

